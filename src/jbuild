(jbuild_version 1)

(library
 ((name high_ollvm)
  (modules_without_implementation (ast))
  (public_name high_ollvm)
  (preprocess (pps (ppx_deriving.std 
                    ppx_enumerate)))
  (libraries (core llvm))))
